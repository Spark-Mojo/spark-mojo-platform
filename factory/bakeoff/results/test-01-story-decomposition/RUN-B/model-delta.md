model: model-delta
test: 01-story-decomposition
run: B
date: 2026-04-09

JAMES NOTE: THIS MODEL FAILED TO PRODUCE ANY RESULT ON THREE TRIES- IT REASONED FOR 15+ MIN PER TRY THAN STOPPED. TRIED THREE DIFFERENT PROVIDERS OF THE MODEL WITH SAME RESULTS. ONLY THING THAT PRESENTED A RESULT WAS TURNING REASONING "OFF"

---

# STORIES.md
**Capability:** Scheduling Mojo
**Produced:** 2026-04-09
**Decomposer:** Story Decomposer hat — Spark Mojo Build Factory
**Guardrails revision applied:** Session 32, April 8, 2026

---

## FOUNDATION LAYER — DocTypes

---

### SCHED-001
**Title:** SM Appointment Type DocType
**Category:** BACKEND
**Size:** XS

**Description:** Create the `SM Appointment Type` DocType with fields: `name`, `duration_minutes`, `is_telehealth`, `buffer_minutes_after`, `default_location_type`.

**Dependencies:** None

**Spec Gates:**
- **Workflow:** Serves Provider Setup workflow — appointment types are the configurable building blocks every other scheduling workflow depends on.
- **CRM Timeline:** N/A — this is a configuration record, not a patient interaction event; no patient relationship is touched.
- **Right Level:** Universal — appointment type configuration is not behavioral-health-specific; any scheduling vertical uses typed appointments.

---

### SCHED-002
**Title:** SM Provider Schedule DocType
**Category:** BACKEND
**Size:** XS

**Description:** Create the `SM Provider Schedule` DocType with fields: `provider_id`, `day_of_week`, `start_time`, `end_time`, `appointment_types_allowed`.

**Dependencies:** SCHED-001

**Spec Gates:**
- **Workflow:** Serves Provider Setup workflow — the weekly template is the authoritative record of when a provider is available and for what appointment types.
- **CRM Timeline:** N/A — this is a provider configuration record; no patient timeline event is generated by schedule template creation.
- **Right Level:** Universal — weekly schedule templates are not behavioral-health-specific; any practice or service business uses provider schedules.

---

### SCHED-003
**Title:** SM Schedule Block DocType
**Category:** BACKEND
**Size:** XS

**Description:** Create the `SM Schedule Block` DocType with fields: `provider_id`, `date`, `start_time`, `end_time`, `reason`.

**Dependencies:** SCHED-002

**Spec Gates:**
- **Workflow:** Serves Provider Setup workflow — individual blocks (vacation, lunch, admin time) override the weekly template for a specific date/time range.
- **CRM Timeline:** N/A — a provider blocking their own time is an internal operational record; no patient relationship is affected.
- **Right Level:** Universal — ad-hoc time blocking is a universal scheduling concern, not vertical-specific.

---

### SCHED-004
**Title:** SM Appointment DocType
**Category:** BACKEND
**Size:** S

**Description:** Create the `SM Appointment` DocType with all fields (`provider_id`, `patient_id`, `appointment_type`, `start_datetime`, `end_datetime`, `status`, `location_type`, `telehealth_link`, `notes`, `cancellation_reason`, `series_id`, `recurring_parent_id`) and the Frappe Workflow definition enforcing the state machine: Requested → Confirmed → In Progress → Completed, with terminal branches to Cancelled and No-Show.

**Dependencies:** SCHED-001, SCHED-002

**Spec Gates:**
- **Workflow:** This IS the appointment lifecycle workflow — it defines every valid state and every valid transition in the Requested → Confirmed → In Progress → Completed / Cancelled / No-Show state machine.
- **CRM Timeline:** N/A at DocType layer — CRM writes are triggered by state transitions and are handled in the MAL endpoint stories (SCHED-009, SCHED-010, SCHED-011, SCHED-012, SCHED-013); the DocType itself does not write to the timeline.
- **Right Level:** Universal — an appointment record with a status lifecycle is not behavioral-health-specific; this structure supports any appointment-driven vertical.

---

### SCHED-005
**Title:** SM Waitlist Entry DocType
**Category:** BACKEND
**Size:** XS

**Description:** Create the `SM Waitlist Entry` DocType with fields: `patient_id`, `provider_id`, `appointment_type`, `requested_date_from`, `requested_date_to`, `notified`.

**Dependencies:** SCHED-001

**Spec Gates:**
- **Workflow:** Serves the Waitlist workflow — when no slots are available, this record captures the patient's place in queue and tracks notification status.
- **CRM Timeline:** N/A at DocType layer — the CRM write for waitlist join happens in the MAL endpoint story (SCHED-020); the DocType itself does not write.
- **Right Level:** Universal — waitlists are a universal scheduling pattern applicable to any service vertical.

---

## PROVIDER SETUP — Backend Endpoints

---

### SCHED-006
**Title:** MAL Provider Schedule Endpoints (GET + POST)
**Category:** BACKEND
**Size:** S

**Description:** Implement `GET /api/modules/scheduling/provider/{id}/schedule` and `POST /api/modules/scheduling/provider/{id}/schedule` on the Mojo Abstraction Layer to read and write a provider's weekly schedule template.

**Dependencies:** SCHED-002

**Spec Gates:**
- **Workflow:** Serves Provider Setup workflow — these are the read/write operations for the weekly availability template that all slot-availability calculations depend on.
- **CRM Timeline:** N/A — reading or writing a provider's schedule template does not constitute a patient interaction event.
- **Right Level:** Universal — provider schedule management is not behavioral-health-specific.

---

### SCHED-007
**Title:** MAL Schedule Block Endpoints (POST + DELETE)
**Category:** BACKEND
**Size:** XS

**Description:** Implement `POST /api/modules/scheduling/provider/{id}/block` and `DELETE /api/modules/scheduling/provider/{id}/block/{block_id}` on the MAL to create and remove individual schedule blocks.

**Dependencies:** SCHED-003

**Spec Gates:**
- **Workflow:** Serves Provider Setup workflow — block creation and deletion modify the effective availability surface that the slot engine reads.
- **CRM Timeline:** N/A — blocking a provider's time is an operational record with no patient relationship.
- **Right Level:** Universal — ad-hoc time blocking is universal to any scheduling system.

---

### SCHED-008
**Title:** MAL Providers List Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `GET /api/modules/scheduling/providers` on the MAL, returning a list of providers with availability metadata (next available slot, schedule configured flag).

**Dependencies:** SCHED-002, SCHED-003

**Spec Gates:**
- **Workflow:** Serves Appointment Booking workflow — the receptionist or patient must be able to discover which providers exist and which have configured availability before selecting a slot.
- **CRM Timeline:** N/A — this is a read-only query endpoint; no event is generated.
- **Right Level:** Universal — provider discovery is a universal scheduling concern.

---

## APPOINTMENT BOOKING — Backend Endpoints

---

### SCHED-009
**Title:** MAL Available Slots Endpoint
**Category:** BACKEND
**Size:** S

**Description:** Implement `GET /api/modules/scheduling/slots` on the MAL, computing available time slots by intersecting the provider's weekly schedule template, existing `SM Appointment` records, and active `SM Schedule Block` records for the requested provider, date range, and appointment type (including buffer time).

**Dependencies:** SCHED-001, SCHED-002, SCHED-003, SCHED-004

**Spec Gates:**
- **Workflow:** Serves Appointment Booking workflow — slot discovery is the first step; without it, no appointment can be booked.
- **CRM Timeline:** N/A — slot querying is read-only; no event is created.
- **Right Level:** Universal — availability calculation is universal to any scheduling vertical.

---

### SCHED-010
**Title:** MAL Appointment Create Endpoint
**Category:** BACKEND
**Size:** S

**Description:** Implement `POST /api/modules/scheduling/appointment/create` on the MAL, validating the requested slot against current availability, creating the `SM Appointment` in `Requested` state, and writing an "Appointment Created" event to the CRM timeline.

**Dependencies:** SCHED-004, SCHED-009

**Spec Gates:**
- **Workflow:** Serves Appointment Booking workflow — this is the action that transitions a slot selection into a named appointment record in the Requested state.
- **CRM Timeline:** Writes "Appointment Created" to the patient's CRM activity timeline including provider name, appointment type, and scheduled datetime.
- **Right Level:** Universal — appointment creation is universal; behavioral health is simply the first vertical using it.

---

### SCHED-011
**Title:** MAL Appointment Read Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `GET /api/modules/scheduling/appointment/{id}` on the MAL, returning the full `SM Appointment` record including current status, telehealth link if present, and all fields.

**Dependencies:** SCHED-004

**Spec Gates:**
- **Workflow:** Serves all appointment lifecycle workflows — every UI component that displays or acts on an appointment fetches via this endpoint.
- **CRM Timeline:** N/A — read-only fetch; no event generated.
- **Right Level:** Universal.

---

## APPOINTMENT LIFECYCLE — Backend Endpoints (one per state transition)

---

### SCHED-012
**Title:** MAL Appointment Confirm Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `PUT /api/modules/scheduling/appointment/{id}/confirm` on the MAL, calling `transition_state()` to move the appointment from `Requested` to `Confirmed` and writing "Appointment Confirmed" to the CRM timeline.

**Dependencies:** SCHED-004, SCHED-010

**Spec Gates:**
- **Workflow:** Serves Appointment Confirmation workflow — this is the single transition that moves an appointment from requested to confirmed and queues the reminder notification trigger.
- **CRM Timeline:** Writes "Appointment Confirmed" to the patient's CRM timeline including provider and scheduled datetime.
- **Right Level:** Universal.

---

### SCHED-013
**Title:** MAL Appointment Start Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `PUT /api/modules/scheduling/appointment/{id}/start` on the MAL, calling `transition_state()` to move the appointment from `Confirmed` to `In Progress`.

**Dependencies:** SCHED-004, SCHED-012

**Spec Gates:**
- **Workflow:** Serves Appointment Execution workflow — the clinician or front desk marks the session as started, opening the In Progress state.
- **CRM Timeline:** N/A — the "In Progress" transition is a clinical operational signal; platform convention treats Created, Confirmed, Completed, Cancelled, and No-Show as the meaningful CRM events; In Progress does not generate a separate timeline entry.
- **Right Level:** Universal.

---

### SCHED-014
**Title:** MAL Appointment Complete Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `PUT /api/modules/scheduling/appointment/{id}/complete` on the MAL, calling `transition_state()` to move the appointment from `In Progress` to `Completed` and writing "Appointment Completed" (including type and duration) to the CRM timeline.

**Dependencies:** SCHED-004, SCHED-013

**Spec Gates:**
- **Workflow:** Serves Appointment Execution workflow — Completed is the billing trigger state; this transition closes the clinical session and signals downstream billing.
- **CRM Timeline:** Writes "Appointment Completed" to the patient's CRM timeline including appointment type and duration in minutes.
- **Right Level:** Universal — completion as billing trigger is a platform vocabulary decision, not behavioral-health-specific.

---

### SCHED-015
**Title:** MAL Appointment Cancel Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `PUT /api/modules/scheduling/appointment/{id}/cancel` on the MAL, accepting a `cancellation_reason` code, calling `transition_state()` to move the appointment to `Cancelled`, and writing "Appointment Cancelled" (including reason) to the CRM timeline.

**Dependencies:** SCHED-004, SCHED-010

**Spec Gates:**
- **Workflow:** Serves Cancellation and No-Show workflow — cancellation is a distinct terminal transition available from both Requested and Confirmed states and requires a reason code.
- **CRM Timeline:** Writes "Appointment Cancelled" to the patient's CRM timeline including the cancellation reason code.
- **Right Level:** Universal.

---

### SCHED-016
**Title:** MAL Appointment No-Show Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `PUT /api/modules/scheduling/appointment/{id}/no-show` on the MAL, calling `transition_state()` to move the appointment from `Confirmed` to `No-Show` and writing "Appointment No-Show" to the CRM timeline.

**Dependencies:** SCHED-004, SCHED-012

**Spec Gates:**
- **Workflow:** Serves Cancellation and No-Show workflow — No-Show is a distinct terminal state from Cancellation with its own CRM event and future reporting implications.
- **CRM Timeline:** Writes "Appointment No-Show" to the patient's CRM timeline including provider name and appointment type.
- **Right Level:** Universal.

---

## WAITLIST — Backend Endpoints

---

### SCHED-017
**Title:** MAL Waitlist Join Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `POST /api/modules/scheduling/waitlist/join` on the MAL, creating an `SM Waitlist Entry` record for a patient + provider + appointment type combination with a requested date range.

**Dependencies:** SCHED-005

**Spec Gates:**
- **Workflow:** Serves Waitlist workflow — when no slots are available, this is the action that places a patient on the waitlist.
- **CRM Timeline:** N/A — waitlist join is a queue entry, not a confirmed interaction; the meaningful CRM event is appointment creation when a slot is filled, not the waitlist entry itself. **FLAG:** James should confirm whether waitlist join warrants its own CRM entry; this decision was not explicit in the research summary.
- **Right Level:** Universal.

---

### SCHED-018
**Title:** MAL Waitlist Remove Endpoint
**Category:** BACKEND
**Size:** XS

**Description:** Implement `DELETE /api/modules/scheduling/waitlist/{id}` on the MAL, removing a patient from the waitlist (used when a patient is booked or withdraws).

**Dependencies:** SCHED-005, SCHED-017

**Spec Gates:**
- **Workflow:** Serves Waitlist workflow — waitlist entries must be clearable when a slot is filled (auto) or when the patient withdraws (manual).
- **CRM Timeline:** N/A — waitlist removal is a queue cleanup operation; the booking event (SCHED-010) handles the patient-facing CRM entry.
- **Right Level:** Universal.

---

## INTEGRATIONS — n8n Workflows (one per trigger)

---

### SCHED-019
**Title:** n8n Appointment Reminder Workflow (24h and 1h)
**Category:** INTEGRATION
**Size:** S

**Description:** Build the n8n workflow that fires on `SM Appointment` reaching `Confirmed` state, schedules two timed notification jobs (24h and 1h before `start_datetime`), and sends email + SMS reminders to the patient including telehealth link if `is_telehealth` is true.

**Dependencies:** SCHED-012

**Spec Gates:**
- **Workflow:** Serves Appointment Confirmation workflow — reminder delivery is the downstream cross-system action triggered by the Confirmed state; n8n owns this per the Frappe/n8n boundary rule.
- **CRM Timeline:** N/A — reminder sends are logged by the CRM/notification layer, not by this scheduling integration; this workflow's responsibility is delivery, not logging. **FLAG:** Confirm whether reminder sends should also write to the CRM timeline; the research summary does not list reminder events as required CRM entries.
- **Right Level:** Universal — timed reminders with configurable lead times are universal to any appointment system.

---

### SCHED-020
**Title:** n8n Telehealth Link Generation Workflow
**Category:** INTEGRATION
**Size:** S

**Description:** Build the n8n workflow that fires when an `SM Appointment` is created with `is_telehealth = true`, calls the video provider API to generate a unique meeting link, and writes the resulting link back to the `SM Appointment`'s `telehealth_link` field via the MAL.

**Dependencies:** SCHED-010

**⚠️ ARCHITECTURE FLAG:** This story requires n8n to write data back to Frappe. Per Immutable Rule 7, n8n NEVER modifies document state. Writing `telehealth_link` to the appointment record is a data update, not a state transition. Resolution options: (A) Expose a dedicated MAL endpoint (`PUT /api/modules/scheduling/appointment/{id}/set-telehealth-link`) that n8n calls, keeping Frappe state transitions internal while allowing n8n to set a data field — this is acceptable if the field write is not a `transition_state()` call; (B) James confirms whether telehealth link is a field update (permitted via MAL) or a state change (not permitted). This story should not be built until this is clarified. Writing a BLOCKED note is recommended if James has not already ruled on this pattern.

**Spec Gates:**
- **Workflow:** Serves Telehealth Link Generation workflow — the video link must be generated and attached to the appointment before reminders fire so that the link can be included in notifications.
- **CRM Timeline:** N/A — link generation is a technical setup step; no patient interaction event occurs.
- **Right Level:** Universal — telehealth link generation is a universal feature of any telehealth-enabled appointment system; the video provider is a configurable spoke.

---

### SCHED-021
**Title:** n8n Billing Trigger Workflow (on Appointment Completion)
**Category:** INTEGRATION
**Size:** XS

**Description:** Build the n8n workflow that fires on `SM Appointment` reaching `Completed` state and kicks off the billing workflow in Healthcare Billing Mojo by posting the appointment ID and relevant metadata to the billing intake endpoint.

**Dependencies:** SCHED-014

**Spec Gates:**
- **Workflow:** Serves Appointment Execution workflow — appointment completion IS the billing trigger per platform vocabulary; this n8n workflow is the cross-system handoff that starts the claim lifecycle.
- **CRM Timeline:** N/A — the CRM write for appointment completion is handled by SCHED-014 (the MAL endpoint); this n8n workflow handles the billing system handoff only.
- **Right Level:** Universal — completion-as-billing-trigger is a platform-level pattern; the Healthcare Billing Mojo is the vertical spoke.

---

### SCHED-022
**Title:** n8n Waitlist Slot-Opening Notification Workflow
**Category:** INTEGRATION
**Size:** S

**Description:** Build the n8n workflow that fires when an `SM Appointment` transitions to `Cancelled` or `No-Show`, queries `SM Waitlist Entry` records matching that provider + appointment type + date range, and sends notification (email + SMS) to each matching waitlisted patient.

**Dependencies:** SCHED-015, SCHED-016, SCHED-017

**Spec Gates:**
- **Workflow:** Serves Waitlist workflow — when a slot opens due to cancellation or no-show, waitlisted patients must be notified promptly so the slot can be refilled.
- **CRM Timeline:** N/A — the notification send itself is a delivery event; the subsequent appointment booking (if the patient takes the slot) generates the CRM entry via SCHED-010.
- **Right Level:** Universal.

---

## FRONTEND — React Components

---

### SCHED-023
**Title:** ProviderScheduleEditor Component
**Category:** FRONTEND
**Size:** S

**Description:** Build the `ProviderScheduleEditor.jsx` React component providing a weekly template management interface where an admin can set available days, hours, and allowed appointment types per day, calling `GET` and `POST /api/modules/scheduling/provider/{id}/schedule`.

**Dependencies:** SCHED-006

**Spec Gates:**
- **Workflow:** Serves Provider Setup workflow — this is the primary UI surface for configuring a provider's recurring availability.
- **CRM Timeline:** N/A — frontend component; CRM writes are handled by the MAL endpoints it calls.
- **Right Level:** Universal — weekly schedule editing is not vertical-specific.

---

### SCHED-024
**Title:** AvailabilityCalendar Component
**Category:** FRONTEND
**Size:** S

**Description:** Build the `AvailabilityCalendar.jsx` React component rendering a weekly view of provider availability with selectable open slots, calling `GET /api/modules/scheduling/slots` with provider, date range, and appointment type filters.

**Dependencies:** SCHED-009, SCHED-023

**Spec Gates:**
- **Workflow:** Serves Appointment Booking workflow — this is the visual slot picker that initiates all new appointment bookings.
- **CRM Timeline:** N/A — frontend component; no direct CRM writes.
- **Right Level:** Universal.

---

### SCHED-025
**Title:** AppointmentBookingForm Component
**Category:** FRONTEND
**Size:** S

**Description:** Build the `AppointmentBookingForm.jsx` React component providing the slot + patient + appointment type picker with a confirmation step, calling `POST /api/modules/scheduling/appointment/create` on submission.

**Dependencies:** SCHED-010, SCHED-024

**Spec Gates:**
- **Workflow:** Serves Appointment Booking workflow — this form completes the booking flow by collecting all required fields and submitting the appointment creation request.
- **CRM Timeline:** N/A — frontend component; the CRM write is handled by SCHED-010 on the MAL.
- **Right Level:** Universal.

---

### SCHED-026
**Title:** AppointmentCard Component
**Category:** FRONTEND
**Size:** S

**Description:** Build the `AppointmentCard.jsx` React component displaying full appointment detail with status-aware action buttons (Confirm, Start, Complete, Cancel, No-Show), calling the appropriate MAL lifecycle endpoint (`/confirm`, `/start`, `/complete`, `/cancel`, `/no-show`) for each action.

**Dependencies:** SCHED-011, SCHED-012, SCHED-013, SCHED-014, SCHED-015, SCHED-016

**Spec Gates:**
- **Workflow:** Serves all appointment lifecycle workflows (Confirmation, Execution, Cancellation, No-Show) — the AppointmentCard is the unified action surface for every post-booking state transition.
- **CRM Timeline:** N/A — frontend component; CRM writes are handled by each respective MAL endpoint.
- **Right Level:** Universal.

---

### SCHED-027
**Title:** WaitlistPanel Component
**Category:** FRONTEND
**Size:** S

**Description:** Build the `WaitlistPanel.jsx` React component allowing a receptionist or patient to view active waitlist entries and remove themselves or a patient from the waitlist, calling `POST /api/modules/scheduling/waitlist/join` and `DELETE /api/modules/scheduling/waitlist/{id}`.

**Dependencies:** SCHED-017, SCHED-018

**Spec Gates:**
- **Workflow:** Serves Waitlist workflow — this panel surfaces the waitlist queue and supports both join and withdrawal actions.
- **CRM Timeline:** N/A — frontend component; no direct CRM writes.
- **Right Level:** Universal.

---

## OPEN FLAGS FOR JAMES

The following items were encountered during decomposition and require James confirmation before the flagged stories are commissioned for build:

| Flag | Story | Question |
|------|-------|----------|
| FLAG-SCHED-A | SCHED-020 | n8n writing `telehealth_link` back to Frappe: is a MAL data-field update endpoint (not a state transition) permitted under Immutable Rule 7, or does this require a different pattern? Story should not be built until confirmed. |
| FLAG-SCHED-B | SCHED-017 | Does joining a waitlist warrant a CRM timeline entry? Research summary did not list it. Recommend James confirms yes/no so SCHED-017 can be finalised. |
| FLAG-SCHED-C | SCHED-019 | Should reminder sends (24h, 1h) write to the CRM timeline? Research summary does not list them as required CRM events but platform Leg 2 could argue they qualify as "every interaction." |
| FLAG-SCHED-D | General | OQ-004 applies here: SM Appointment needs a state log. Should the generic SM State Log be built first, or should this capability ship its own `SM Appointment State Log` DocType as acceptable per-DocType debt? |

---

---

# DEPENDENCY-GRAPH.md
**Capability:** Scheduling Mojo
**Produced:** 2026-04-09

---

## Build Order and Parallel Execution Groups

Stories within the same group have no inter-group dependencies and can be built in parallel. A group may not start until all stories in the preceding group are complete.

---

### GROUP 1 — Foundation (No Dependencies)
> Build these first. Everything else depends on at least one of them.

| Story | Title | Category | Size |
|-------|-------|----------|------|
| SCHED-001 | SM Appointment Type DocType | BACKEND | XS |

---

### GROUP 2 — Core DocTypes (Depends on SCHED-001)
> Can all be built in parallel once GROUP 1 is complete.

| Story | Title | Category | Size |
|-------|-------|----------|------|
| SCHED-002 | SM Provider Schedule DocType | BACKEND | XS |
| SCHED-005 | SM Waitlist Entry DocType | BACKEND | XS |

---

### GROUP 3 — Remaining DocTypes (Depends on SCHED-002)
> Can be built in parallel once GROUP 2 is complete.

| Story | Title | Category | Size |
|-------|-------|----------|------|
| SCHED-003 | SM Schedule Block DocType | BACKEND | XS |
| SCHED-004 | SM Appointment DocType + Workflow | BACKEND | S |

---

### GROUP 4 — First-Wave MAL Endpoints (Depends on SCHED-002, SCHED-003, SCHED-004, SCHED-005)
> All can be built in parallel once GROUP 3 is complete.

| Story | Title | Category | Size |
|-------|-------|----------|------|
| SCHED-006 | MAL Provider Schedule Endpoints (GET + POST) | BACKEND | S |
| SCHED-007 | MAL Schedule Block Endpoints (POST + DELETE) | BACKEND | XS |
| SCHED-011 | MAL Appointment Read Endpoint | BACKEND | XS |
| SCHED-017 | MAL Waitlist Join Endpoint | BACKEND | XS |

---

### GROUP 5 — Second-Wave MAL Endpoints (Mixed Dependencies)
> Can be built in