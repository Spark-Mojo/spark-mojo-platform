[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "sm_provisioning"
authors = [{name = "Spark Mojo"}]
dynamic = ["version", "description"]

[tool.flit.module]
name = "sm_provisioning"
